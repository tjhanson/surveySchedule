import React, {useEffect, useRef} from 'react';
import {useSelector,useDispatch } from 'react-redux';



function BmpPane() {

    //const site = useSelector((state) => state.site.currentSite)

    return(
        <></>
    )
}

export default BmpPane;